No console do wsl2 > Ubuntu

//baixa instalar imagem no Ubuntu
docker pull mariadb:10.4

//exibir imagens instaladas
docker images


//Criar container do mariaDB (ficara disponível)
docker run --name mariadb -e MYSQL_ROOT_PASSWORD=admin123 -p 3306:3306 -d docker.io/library/mariadb:10.4
docker run --name mariadb -e MYSQL_ROOT_PASSWORD=root123 -p 3306:3306 -d docker.io/library/mariadb:10.4 --add-host=host.docker.internal:host-gateway --restart always

//recordar o password para root

//Fazer com o que o container sempre suba junto com o docker (ao iniciar wsl/ubuntu: sudo service docker start)
docker update --restart always mariadb

//Descobrir IP do container do mariadb
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' mariadb

//Conectar no console do docker
docker exec -t mariadb
apt-get update
apt-get upgrade
apt-get install nano
cd /etc/mysql/
nano my.cnf

==>localizar linha de bindadress remover # e deixar = 0.0.0.0

ctrl + X
Y
<enter>

ctrl+D para sair do console do container

//reiniciar o mariadb
docker stop mariadb
dockar start mariadb

mysql -h 172.17.0.2 -P 3306 --protocol=TCP -u root -p

